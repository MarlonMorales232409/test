# City Finder using MapBox and React

To try the project clone the repository and then run ```npm install``` to install all dependencies

then ```cd City_Finder``` and type ```npm run start```


# test
